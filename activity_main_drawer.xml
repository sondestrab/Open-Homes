<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_camera"
            android:icon="@drawable/account"
            android:title="Account" />

        <item
            android:id="@+id/nav_slideshow"
            android:icon="@drawable/saved"
            android:title="Saved offers" />
        <item
            android:id="@+id/nav_manage"
            android:icon="@drawable/suggestion"
            android:title="Offers you might like" />
        <item
            android:id="@+id/history"
            android:icon="@drawable/history"
            android:title="History" />
    </group>

    <item android:title="Communicate">
        <menu>
            <item
                android:id="@+id/nav_share"
                android:icon="@drawable/ic_menu_share"
                android:title="Share with your friends" />
            <item
                android:id="@+id/nav_send"
                android:icon="@drawable/ic_menu_send"
                android:title="Send" />
            <item
                android:id="@+id/nav_position"
                android:icon="@drawable/gmap"
                android:title="Your position" />
        </menu>
    </item>

</menu>
